name: esm3
channels:
    - conda-forge
    - nodefaults
dependencies:
    - python>=3.10,<3.13
    - pandas
    - tqdm
    - IProgress
    - ipywidgets
    - ipykernel
    - seaborn
    - plotly
    - cmake
    - pyarrow
    - scikit-learn
    - huggingface_hub
    - pooch
    - pip
    - pip:
        - esm # New ESM module
        - flash-attn --no-build-isolation
        - biopython
        - python-Levenshtein
        - ensembl-rest
